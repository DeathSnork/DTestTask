version: '3.1'
services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: maugami
      POSTGRES_PASSWORD: maugami
      POSTGRES_DB: testDB
  web:
    build: .
    ports:
    - 8080:8080

